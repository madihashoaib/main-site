import AdminPage from "@/components/AdminPage";

export const metadata = {
  title: "Admin — adorn N adobe",
  robots: { index: false, follow: false }
};

export default function Admin() {
  return <AdminPage />;
}
